name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
jobs:
  cpp-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository 
        uses: actions/checkout@v4
   
      - name: C++ dependencies install
        run: |
          sudo apt update
          sudo apt install -y \
            g++ \
            cmake \
            libpqxx-dev \
            libpq-dev \
<<<<<<< HEAD
            clang-format
=======
            clang-format \
            cppcheck

      - name: Check code formatting
        run: clang-format --dry-run --Werror src/*.cpp include/*.h

      - name: Static analysis
        run: cppcheck --enable=all --error-exitcode=1 src/ include/
>>>>>>> parent of d23a0a5 (Removed unnecessary tests.)

      - name: Configure CMake
        run: cmake -S . -B build

      - name: Build C++ application
        run: cmake --build build

  docker-validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate docker-compose configuration
        run: docker compose config

        
